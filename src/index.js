import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App>{console.log(`FrontEnd-Web is running at ${new Date()}`)}</App>, document.getElementById('root'));
